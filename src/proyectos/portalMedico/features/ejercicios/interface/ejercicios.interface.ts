// Separado del .ts base del componente según Specs.md: los componentes no
// llevan interfaces, enums ni constantes.

/** Qué está haciendo la pantalla: viendo el catálogo, creando o editando. */
export type ModoEjercicios = 'listado' | 'crear' | 'editar';
